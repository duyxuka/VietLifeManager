<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <location path="." inheritInChildApplications="false">
    <system.webServer>
      <!-- ASP.NET Core handler -->
      <handlers>
        <add name="aspNetCore" path="*" verb="*" modules="AspNetCoreModuleV2" resourceType="Unspecified" />
      </handlers>
      <!-- ASP.NET Core process -->
      <aspNetCore processPath=".\VietLife.HttpApi.Host.exe" arguments=".\VietLife.HttpApi.Host.dll" stdoutLogEnabled="true" stdoutLogFile=".\Logs\stdout" hostingModel="inprocess">
        <environmentVariables>
          <environmentVariable name="ASPNETCORE_ENVIRONMENT" value="Production" />
          <environmentVariable name="ASPNETCORE_URLS" value="https://vietlife.com.vn" />
        </environmentVariables>
      </aspNetCore>
      <!-- Xóa header mặc định -->
      <httpProtocol>
        <customHeaders>
          <remove name="X-Powered-By" />
          <add name="X-Forwarded-Proto" value="http" />
        </customHeaders>
      </httpProtocol>
      <!-- ============================= -->
      <!-- 🔁 URL Rewrite Configuration -->
      <!-- ============================= -->
      <rewrite>
        <rules>
          <clear />
          <!-- HTTPS nhưng bỏ qua sitemap -->
          <rule name="Redirect HTTP to HTTPS" stopProcessing="true">
            <match url="(.*)" />
            <conditions logicalGrouping="MatchAll">
              <add input="{HTTPS}" pattern="^OFF$" />
              <add input="{URL}" pattern="^/(sitemap\.xml|robots\.txt)$" negate="true" />
            </conditions>
            <action type="Redirect" url="https://vietlife.com.vn/{R:1}" redirectType="Permanent" />
          </rule>

          <!-- Static files (THÊM xml) -->
          <rule name="Static Files" stopProcessing="true">
            <match url="([\S]+[.](jpg|jpeg|gif|css|png|js|mjs|json|wasm|ico|html|map|svg|xml|woff|woff2|ttf|eot|otf))" />
            <action type="None" />
          </rule>

          <!-- API -->
          <rule name="Backend APIs" stopProcessing="true">
            <match url=".*" />
            <conditions logicalGrouping="MatchAny">
              <add input="{REQUEST_URI}" pattern="^/(api|swagger|health-status|health-ui|health-ui-api|signalr|\.well-known|connect|Account|AbpApplicationConfiguration)" />
            </conditions>
            <action type="None" />
          </rule>

        </rules>
      </rewrite>
      <!-- ============================= -->
      <!-- ⚙️ Security -->
      <!-- ============================= -->
      <security>
        <requestFiltering allowDoubleEscaping="true" />
      </security>
      <!-- ============================= -->
      <!-- 🧩 Static Content / MIME Types -->
      <!-- ============================= -->
      <staticContent>
        <clientCache cacheControlMode="UseMaxAge" />
        <!-- Remove trùng -->
        <remove fileExtension=".svg" />
        <remove fileExtension=".eot" />
        <remove fileExtension=".ttf" />
        <remove fileExtension=".woff" />
        <remove fileExtension=".woff2" />
        <remove fileExtension=".otf" />
        <remove fileExtension=".js" />
        <remove fileExtension=".mjs" />
        <remove fileExtension=".json" />
        <remove fileExtension=".xml" />
        <!-- ⚠️ Thêm dòng này -->
        <remove fileExtension=".wasm" />
        <!-- Thêm lại MIME cần thiết -->
        <mimeMap fileExtension=".xml" mimeType="application/xml" />
        <mimeMap fileExtension=".ttf" mimeType="application/octet-stream" />
        <mimeMap fileExtension=".svg" mimeType="image/svg+xml" />
        <mimeMap fileExtension=".eot" mimeType="application/vnd.ms-fontobject" />
        <mimeMap fileExtension=".woff" mimeType="application/x-woff" />
        <mimeMap fileExtension=".woff2" mimeType="application/x-woff" />
        <mimeMap fileExtension=".otf" mimeType="application/otf" />
        <mimeMap fileExtension=".js" mimeType="application/javascript" />
        <mimeMap fileExtension=".mjs" mimeType="application/javascript" />
        <mimeMap fileExtension=".json" mimeType="application/json" />
        <mimeMap fileExtension=".wasm" mimeType="application/wasm" />
      </staticContent>
      <!-- ============================= -->
      <!-- 📄 Default Document -->
      <!-- ============================= -->
      <defaultDocument>
        <files>
          <clear />
          <add value="index.html" />
        </files>
      </defaultDocument>
    </system.webServer>
  </location>
</configuration>
<!--ProjectGuid: 96e5259e-59a3-4faf-bcd1-5bf8e74ef82c-->